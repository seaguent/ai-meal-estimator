# Backend environment example
# Copy to .env and fill in values

GEMINI_API_KEY=replace_me
GEMINI_MODEL=gemini-2.0-flash

# Comma-separated list (no spaces) of allowed origins for CORS (dev default shown)
ALLOWED_ORIGINS=http://localhost:5173

# Set to 1 to run without a real Gemini key (returns mock data)
# Recommended while building UI or if API quota exhausted.
# USE_MOCK_GEMINI=1
USE_MOCK_GEMINI=0
